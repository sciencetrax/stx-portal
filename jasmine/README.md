# The `jasmine` Directory

## Overview

This is only used by WebStorm.  Without these files here, WebStorm thinks that it
needs these files and continually annoys you with the recommendation to add them.